<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="android:activatedBackgroundIndicator">@color/primaryLightColor</item>
    </style>

    <style name="MyToolbarTheme"
        parent="@style/Theme.AppCompat.NoActionBar">
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
        parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="PopupMenu">
        <item name="android:listDivider">@color/grey</item>
        <item name="colorControlHighlight">@color/primaryLightColor </item>
    </style>

    <style name="ButtonTheme" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/primaryLightColor</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="spinnerTheme">
        <item name="android:textColor">@color/textColor</item>
        <item name="android:background">@color/textBGColor</item>
    </style>





</resources>
